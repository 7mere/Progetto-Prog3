package com.unito.server.models;

public class ServerStorage {
    // Questa classe rappresenta lo stato persistente del server e la logica di dominio, ad esempio potrebbe contenere una lista di email, una lista di utenti, ecc.
    // In questo modo, i thread che gestiscono le connessioni dei client possono accedere a questo modello condiviso per leggere e modificare lo stato del server in modo thread-safe (ad esempio usando synchronized o altre tecniche di sincronizzazione se necessario).

    public class ServerStorage {
        
    }
}
